export class ScansDownloader {

  public async downloadAllMissingCards(): Promise<void> {
    // Missing scans now fall back to GitHub image URLs from card-data-finder.
    return Promise.resolve();
  }

}
